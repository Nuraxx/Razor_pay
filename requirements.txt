setuptools==75.6.0
fastapi==0.115.6
starlette==0.41.3  # explicit -- see the Day-13 note below on why this exact version matters
uvicorn[standard]==0.34.0
razorpay==1.4.2
sqlalchemy==2.0.36
python-dotenv==1.0.1
pytest==8.3.4
httpx==0.28.1

# Day 3 -- synthetic dataset generation
pandas==3.0.5
numpy==2.5.2

# Day 4 -- model training, calibration, evaluation
scikit-learn==1.9.0
catboost==1.2.10
matplotlib==3.11.1
scipy==1.18.1
joblib==1.5.3

# Day 11 -- LLM-assisted communication layer
anthropic==0.125.0
pydantic==2.13.4

# Day 13 -- dashboard UI. Pinned to 1.56.0 specifically: newer Streamlit
# releases (1.57+) unconditionally import an optional starlette-based ASGI
# server at package-import time, which needs starlette>=0.46 -- directly
# incompatible with fastapi==0.115.6's starlette<0.42.0 pin above. 1.56.0 is
# the newest release where that starlette integration is still an opt-in
# "extra" (not imported by default), so it coexists with the existing
# starlette==0.41.3 already pinned by fastapi. Verified: `pip check` clean,
# full Day 1-12 test suite (358/358) unaffected.
streamlit==1.56.0
plotly==6.9.0
